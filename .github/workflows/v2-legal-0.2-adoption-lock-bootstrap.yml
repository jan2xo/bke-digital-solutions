name: V2 Legal 0.2 Adoption Lock Bootstrap

on:
  push:
    branches: [feat/v2-legal-checkout-requirements-adoption]
    paths:
      - ".github/workflows/v2-legal-0.2-adoption-lock-bootstrap.yml"

permissions:
  contents: write

jobs:
  lock:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Pin certified Legal release
        shell: bash
        run: |
          set -euo pipefail
          node <<'NODE'
          const fs = require('fs');
          const path = 'package.json';
          const pkg = JSON.parse(fs.readFileSync(path, 'utf8'));
          pkg.dependencies['@bke/legal'] = 'https://github.com/jan2xo/bke-libraries-typescript/releases/download/legal-v0.2.0/bke-legal-0.2.0.tgz';
          fs.writeFileSync(path, JSON.stringify(pkg, null, 2) + '\n');
          NODE
          npm install --package-lock-only --ignore-scripts
      - name: Commit package pin and lock
        shell: bash
        run: |
          set -euo pipefail
          rm .github/workflows/v2-legal-0.2-adoption-lock-bootstrap.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package.json package-lock.json .github/workflows/v2-legal-0.2-adoption-lock-bootstrap.yml
          git commit -m "build(v2 legal): pin @bke/legal 0.2.0"
          git push origin HEAD:feat/v2-legal-checkout-requirements-adoption

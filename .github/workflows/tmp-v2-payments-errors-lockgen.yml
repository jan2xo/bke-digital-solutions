name: TEMP V2 Payments 0.2.0 Lockfile Generation

on:
  push:
    branches:
      - tmp/v2-payments-errors-0.2.0

permissions:
  contents: write

jobs:
  lockfile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: tmp/v2-payments-errors-0.2.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Regenerate lockfile from released Payments artifact
        run: npm install --package-lock-only --ignore-scripts
      - name: Commit generated lockfile when changed
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet -- package-lock.json; then
            echo "package-lock.json already current"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package-lock.json
          git commit -m "chore(v2 payments): regenerate 0.2.0 lockfile"
          git push origin HEAD:tmp/v2-payments-errors-0.2.0

import { ResetPasswordForm } from "@/components/password-reset-forms";
export default async function ResetPasswordPage({searchParams}:{searchParams:Promise<{token?:string}>}){const{token=""}=await searchParams;return <section className="mx-auto max-w-md px-4 py-16"><h1 className="mb-8 text-4xl font-black">Choose a new password</h1>{token?<ResetPasswordForm token={token}/>:<p>This reset link is invalid.</p>}</section>}

import{db}from"@/lib/db";import{AdminTable}from"@/components/admin-table";import{AdminActionButton}from"@/components/admin-action-button";export default async function Releases(){const rows=await db.productVersion.findMany({include:{product:true,artifacts:true},orderBy:{releasedAt:"desc"},take:250});return <section className="shell py-10"><h1 className="mb-7 text-4xl font-black">Release center</h1><AdminTable headers={["Release","Product","Channel","Published","Assets","Released","Actions"]} rows={rows.map(v=>[v.version,v.product.name,v.channel,v.active?v.deprecatedAt?"Deprecated":"Published":"Draft",String(v.artifacts.length),v.releasedAt.toLocaleString(),[<AdminActionButton key="p" url={`/api/admin/versions/${v.id}`} label={v.active?"Unpublish":"Publish"} body={{published:!v.active}}/>,<AdminActionButton key="b" url={`/api/admin/versions/${v.id}`} label={v.channel==="BETA"?"Make stable":"Make beta"} body={{channel:v.channel==="BETA"?"STABLE":"BETA"}}/>,<AdminActionButton key="r" url={`/api/admin/versions/${v.id}`} label="Rollback to" body={{rollback:true}} confirmText="Make this the latest active release?"/>,<AdminActionButton key="d" url={`/api/admin/versions/${v.id}`} label={v.deprecatedAt?"Undeprecate":"Deprecate"} body={{deprecated:!v.deprecatedAt}} danger={!v.deprecatedAt}/>]])}/></section>}

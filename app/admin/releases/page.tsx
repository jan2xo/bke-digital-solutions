import { db } from "@/lib/db";
import { AdminTable } from "@/components/admin-table";
import { AdminActionButton } from "@/components/admin-action-button";
export default async function Releases() { const rows = await db.productVersion.findMany({ include: { product: true, artifacts: true, approvals: { orderBy: { createdAt: "desc" }, take: 1 }, supplyChainEvidence: true }, orderBy: { releasedAt: "desc" }, take: 250 }); return <section className="shell py-10"><h1 className="mb-2 text-4xl font-black">Release management</h1><p className="mb-7 text-slate-600">Promotions are forward-only and stable/LTS require an explicit administrator approval. Certificate and malware states remain evidence-driven.</p><AdminTable headers={["Release", "Product", "Lifecycle", "Approval", "Integrity", "Assets", "Actions"]} rows={rows.map(v => [v.version, v.product.name, v.lifecycle, v.approvals[0]?.approvedAt ? "Approved" : "Pending", v.supplyChainEvidence ? `${v.supplyChainEvidence.malwareStatus} / ${v.supplyChainEvidence.signatureVerified ? "signed" : "unsigned"}` : "Missing evidence", String(v.artifacts.length), [<AdminActionButton key="p" url={`/api/admin/versions/${v.id}`} label="Promote" body={{ lifecycle: v.lifecycle === "DRAFT" ? "INTERNAL" : v.lifecycle === "INTERNAL" ? "ALPHA" : v.lifecycle === "ALPHA" ? "BETA" : v.lifecycle === "BETA" ? "RELEASE_CANDIDATE" : "STABLE", approve: v.lifecycle === "RELEASE_CANDIDATE" }} confirmText="Promote this release?"/>]])}/></section>; }

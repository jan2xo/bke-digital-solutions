import { db } from "@/lib/db";
export default async function CommercialSigningKeys() { const keys = await db.commercialSigningKey.findMany({ orderBy: { createdAt: "desc" }, select: { keyId: true, algorithm: true, status: true, createdAt: true, activatedAt: true, retiredAt: true } }); return <main className="shell py-10"><h1 className="text-4xl font-black">Commercial signing keys</h1><p className="mt-2 text-slate-600">Registry status only. Private-key provisioning remains deployment-level and is never accepted by this UI.</p><div className="card mt-8 overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr><th className="p-3">Key ID</th><th className="p-3">Algorithm</th><th className="p-3">Status</th><th className="p-3">Activated</th><th className="p-3">Retired</th></tr></thead><tbody>{keys.map((key) => <tr className="border-t" key={key.keyId}><td className="p-3 font-mono">{key.keyId}</td><td className="p-3">{key.algorithm}</td><td className="p-3">{key.status}</td><td className="p-3">{key.activatedAt?.toLocaleString() ?? "—"}</td><td className="p-3">{key.retiredAt?.toLocaleString() ?? "—"}</td></tr>)}</tbody></table></div></main>; }

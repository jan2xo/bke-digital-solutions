import{notFound}from"next/navigation";import{db}from"@/lib/db";export default async function AdminInvoice({params}:{params:Promise<{id:string}>}){const{id}=await params;const invoice=await db.invoice.findUnique({where:{id},include:{lines:true,order:{include:{account:true,payments:true}}}});if(!invoice)notFound();return <section className="mx-auto max-w-3xl px-4 py-10"><p className="font-bold text-[#3D75A7]">Commercial invoice</p><h1 className="text-4xl font-black">{invoice.number}</h1><p>{invoice.order.account.displayName} · Order {invoice.order.number} · {invoice.status}</p><div className="card mt-7 overflow-hidden"><table className="w-full"><tbody>{invoice.lines.map(l=><tr className="border-b" key={l.id}><td className="p-4">{l.description}</td><td className="p-4">{l.quantity}</td><td className="p-4 text-right">{money(l.totalMinor,invoice.currency)}</td></tr>)}<tr className="font-black"><td className="p-4" colSpan={2}>Total</td><td className="p-4 text-right">{money(invoice.totalMinor,invoice.currency)}</td></tr></tbody></table></div></section>}function money(n:number,c:string){return new Intl.NumberFormat("en-PH",{style:"currency",currency:c}).format(n/100)}

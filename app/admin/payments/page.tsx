import { db } from "@/lib/db";
import { AdminTable } from "@/components/admin-table";
import { ReconcileButton, WebhookAction } from "@/components/admin-payment-actions";
export default async function Payments(){const[attempts,webhooks,reconciliations,refunds]=await Promise.all([db.paymentAttempt.findMany({include:{order:true},orderBy:{createdAt:"desc"},take:100}),db.webhookEvent.findMany({orderBy:{receivedAt:"desc"},take:100}),db.paymentReconciliation.findMany({include:{order:true},orderBy:{createdAt:"desc"},take:100}),db.refundOperation.findMany({include:{order:true},orderBy:{createdAt:"desc"},take:100})]);return <section className="shell space-y-8 py-10"><div><h1 className="text-4xl font-black">Payment operations</h1><p className="mt-2 text-muted">Safe PayMongo lifecycle evidence. Raw provider payloads and secrets are never displayed.</p></div><section><h2 className="mb-3 text-2xl font-black">Attempts</h2><AdminTable headers={["Order","Provider","Status","Created","Action"]} rows={attempts.map(x=>[x.order.number,x.provider,x.status,x.createdAt.toLocaleString(),<ReconcileButton key={x.id} orderId={x.orderId}/>])}/></section><section><h2 className="mb-3 text-2xl font-black">Webhook deliveries</h2><AdminTable headers={["Event","Status","Error","Attempts","Resolution","Action"]} rows={webhooks.map(x=>[x.eventType,x.status,x.lastErrorCode??"—",String(x.processingAttempts),x.resolutionStatus,x.status==="FAILED"&&x.normalizedData?<WebhookAction key={x.id} id={x.id} action="RETRY"/>:x.resolutionStatus==="OPEN"?<WebhookAction key={x.id} id={x.id} action="ACKNOWLEDGE"/>:"—"])}/></section><section><h2 className="mb-3 text-2xl font-black">Reconciliation evidence</h2><AdminTable headers={["Order","Classification","Local","Provider","Status","Created"]} rows={reconciliations.map(x=>[x.order.number,x.classification,x.localStatus,x.providerStatus??"—",x.status,x.createdAt.toLocaleString()])}/></section><section><h2 className="mb-3 text-2xl font-black">Refund operations</h2><AdminTable headers={["Order","Amount","Reason","Status","Created"]} rows={refunds.map(x=>[x.order.number,`${x.currency} ${(x.amountMinor/100).toLocaleString()}`,x.reasonCode,x.status,x.createdAt.toLocaleString()])}/></section></section>}

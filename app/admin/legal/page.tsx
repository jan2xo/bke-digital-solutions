import { AdminLegalCenter } from "@/components/admin-legal-center";
import { db } from "@/lib/db";
export default async function LegalAdminPage() { const documents = await db.legalDocument.findMany({ include: { currentPublishedVersion: true, versions: { orderBy: { versionNumber: "desc" }, include: { author: { select: { name: true, email: true } }, _count: { select: { acceptances: true } }, acceptances: { orderBy: { acceptedAt: "desc" }, take: 20, select: { id: true, acceptanceContext: true, acceptedAt: true, user: { select: { name: true, email: true } }, customerAccount: { select: { displayName: true } } } } } } }, orderBy: { title: "asc" } }); return <main className="shell py-10"><h1 className="text-4xl font-black">Legal &amp; Compliance</h1><p className="mb-8 mt-2 text-slate-600">Manage versioned legal templates and acceptance history. Template content is not legal advice.</p><AdminLegalCenter initialDocuments={documents.map((document) => ({ ...document, versions: document.versions.map((version) => ({ ...version, effectiveAt: version.effectiveAt?.toISOString() ?? null, publishedAt: version.publishedAt?.toISOString() ?? null, createdAt: version.createdAt.toISOString(), updatedAt: version.updatedAt.toISOString(), acceptances: version.acceptances.map((acceptance) => ({ ...acceptance, acceptedAt: acceptance.acceptedAt.toISOString() })) })) }))}/></main>; }

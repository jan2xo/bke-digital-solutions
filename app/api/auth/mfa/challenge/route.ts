import { NextResponse } from "next/server";
import { z } from "zod";
import { ADMIN_EMAIL_CHALLENGE_COOKIE, completeLoginChallenge } from "@/lib/admin-mfa";
import { apiError } from "@/v2/apps/web/http/api-error";
import { assertSameOrigin, clientIp } from "@/v2/apps/web/http/request";
import { rateLimit } from "@/v2/apps/web/http/rate-limit";
import { securityEvent } from "@/v2/apps/web/security/events";
export async function POST(request:Request){try{assertSameOrigin(request);if(!(await rateLimit(`admin-mfa:${clientIp(request)}`,8,900)).allowed){await securityEvent("SECURITY_RATE_LIMIT_TRIGGERED",request,undefined,{reason:"mfa"});throw new Error("RATE_LIMITED")}const{code}=z.object({code:z.string().min(6).max(32)}).parse(await request.json());const result=await completeLoginChallenge(code,request);await securityEvent(result.recoveryUsed?"MFA_RECOVERY_USED":"MFA_CHALLENGE_SUCCEEDED",request,result.userId,undefined,{sessionId:result.sessionId,authenticationMethod:result.authenticationMethod});await securityEvent("ADMIN_LOGIN_SUCCEEDED",request,result.userId,undefined,{sessionId:result.sessionId,authenticationMethod:result.authenticationMethod});await securityEvent("ADMIN_SESSION_CREATED",request,result.userId,undefined,{sessionId:result.sessionId,authenticationMethod:result.authenticationMethod});const response=NextResponse.json({ok:true});response.cookies.delete(ADMIN_EMAIL_CHALLENGE_COOKIE);return response}catch(error){if(!(error instanceof Error&&error.message==="INVALID_MFA_CODE"))await securityEvent("MFA_CHALLENGE_FAILED",request).catch(()=>undefined);return apiError(error)}}

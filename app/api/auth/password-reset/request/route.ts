import { NextResponse } from "next/server";import { db } from "@/lib/db";import { emailSchema } from "@/v2/apps/web/http/validation";import { assertSameOrigin,clientIp } from "@/lib/security/request";import { rateLimit } from "@/lib/security/rate-limit";import { hashToken,randomToken } from "@/lib/security/crypto";import { sendPasswordReset } from "@/lib/email";
export async function POST(request:Request){assertSameOrigin(request);const email=emailSchema.parse((await request.json()).email);if(!(await rateLimit(`reset:${clientIp(request)}:${email}`,5,3600)).allowed)return NextResponse.json({ok:true});const user=await db.user.findUnique({where:{email}});if(user){const token=randomToken();await db.passwordResetToken.create({data:{userId:user.id,tokenHash:hashToken(token),expiresAt:new Date(Date.now()+30*60_000)}});await sendPasswordReset(email,token)}return NextResponse.json({ok:true})}

import { NextResponse } from "next/server";import { requireUser } from "@/lib/auth";import { db } from "@/lib/db";import { assertSameOrigin } from "@/lib/security/request";import { apiError } from "@/lib/http";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){try{assertSameOrigin(request);const user=await requireUser();const{id}=await params;const order=await db.order.findFirst({where:{id,status:"PENDING",account:{OR:[{ownerId:user.id},{memberships:{some:{userId:user.id,role:{in:["OWNER","BILLING"]}}}}]}}});if(!order)throw new Error("NOT_FOUND");await db.$transaction([db.order.update({where:{id},data:{status:"CANCELLED"}}),db.paymentAttempt.updateMany({where:{orderId:id,status:{in:["CREATING","PENDING"]}},data:{status:"CANCELLED"}})]);return NextResponse.json({status:"CANCELLED"})}catch(e){return apiError(e)}}

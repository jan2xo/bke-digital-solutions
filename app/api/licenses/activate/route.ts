import { NextResponse } from "next/server";
import { db } from "@/lib/db";
import { activationSchema } from "@/lib/validation";
import { hashLicenseKey, sha256 } from "@/lib/security/crypto";
import { clientIp } from "@/lib/security/request";
import { rateLimit } from "@/lib/security/rate-limit";
export async function POST(request:Request){try{const input=activationSchema.parse(await request.json());if(!(await rateLimit(`activate:${clientIp(request)}:${input.licenseKey.slice(-4)}`,20,3600)).allowed)return NextResponse.json({error:"RATE_LIMITED"},{status:429});const keyHash=hashLicenseKey(input.licenseKey);const result=await db.$transaction(async tx=>{const license=await tx.license.findUnique({where:{keyHash}});if(!license||license.status!=="ACTIVE"||(license.expiresAt&&license.expiresAt<new Date()))throw new Error("INVALID_LICENSE");const deviceHash=sha256(input.deviceId);const existing=await tx.deviceActivation.findUnique({where:{licenseId_deviceHash:{licenseId:license.id,deviceHash}}});if(existing?.active)return {licenseId:license.publicId,expiresAt:license.expiresAt,status:license.status};const active=await tx.deviceActivation.count({where:{licenseId:license.id,active:true}});const max=license.maxSeats*license.maxDevicesPerSeat;if(active>=max)throw new Error("ACTIVATION_LIMIT");await tx.deviceActivation.upsert({where:{licenseId_deviceHash:{licenseId:license.id,deviceHash}},create:{licenseId:license.id,deviceHash,label:input.label},update:{active:true,deactivatedAt:null,label:input.label}});await tx.licenseEvent.create({data:{licenseId:license.id,type:"ACTIVATED",metadata:{deviceHash}}});return {licenseId:license.publicId,expiresAt:license.expiresAt,status:license.status}},{isolationLevel:"Serializable"});return NextResponse.json(result)}catch(e){const code=e instanceof Error&&e.message==="ACTIVATION_LIMIT"?"ACTIVATION_LIMIT":"INVALID_LICENSE";return NextResponse.json({error:code},{status:code==="ACTIVATION_LIMIT"?409:400})}}

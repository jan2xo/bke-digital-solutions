import{NextResponse}from"next/server";import{requireRecentAdmin}from"@/lib/auth";import{db}from"@/lib/db";import{assertSameOrigin}from"@/v2/apps/web/http/request";import{audit}from"@/lib/audit";import{apiError}from"@/v2/apps/web/http/api-error";
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){try{assertSameOrigin(request);const admin=await requireRecentAdmin();const{id}=await params;const device=await db.deviceActivation.update({where:{id},data:{active:false,deactivatedAt:new Date()},include:{license:true}});await audit({actorId:admin.id,accountId:device.license.accountId,action:"DEVICE_DEACTIVATED",targetType:"DeviceActivation",targetId:id});return NextResponse.json({ok:true})}catch(e){return apiError(e)}}

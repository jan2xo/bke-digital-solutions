import{NextResponse}from"next/server";import{requireAdmin}from"@/lib/auth";import{db}from"@/lib/db";import{assertSameOrigin}from"@/lib/security/request";import{audit}from"@/lib/audit";import{apiError}from"@/lib/http";
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){try{assertSameOrigin(request);const admin=await requireAdmin();const{id}=await params;const device=await db.deviceActivation.update({where:{id},data:{active:false,deactivatedAt:new Date()},include:{license:true}});await audit({actorId:admin.id,accountId:device.license.accountId,action:"DEVICE_DEACTIVATED",targetType:"DeviceActivation",targetId:id});return NextResponse.json({ok:true})}catch(e){return apiError(e)}}

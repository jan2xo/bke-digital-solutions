import{NextResponse}from"next/server";import{createHash,randomUUID}from"node:crypto";import{extname}from"node:path";import{requireAdmin}from"@/lib/auth";import{db}from"@/lib/db";import{assertSameOrigin}from"@/lib/security/request";import{uploadObject,deleteObject}from"@/lib/storage";import{audit}from"@/lib/audit";import{apiError}from"@/lib/http";
const allowed=new Set([".exe",".msi",".dmg",".pkg",".zip",".deb",".appimage"]);const MAX=250*1024*1024;
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){try{assertSameOrigin(request);const admin=await requireAdmin();const{id}=await params;const form=await request.formData();const file=form.get("installer");if(!(file instanceof File)||file.size<1||file.size>MAX)throw new Error("INVALID_FILE_SIZE");const ext=extname(file.name).toLowerCase();if(!allowed.has(ext))throw new Error("INVALID_FILE_TYPE");const current=await db.productArtifact.findUniqueOrThrow({where:{id}});const bytes=new Uint8Array(await file.arrayBuffer());const sha256=createHash("sha256").update(bytes).digest("hex");const objectKey=`products/${current.productId}/replacements/${randomUUID()}${ext}`;await uploadObject(objectKey,bytes,file.type||"application/octet-stream");const artifact=await db.productArtifact.update({where:{id},data:{name:file.name,objectKey,sha256,sizeBytes:file.size,contentType:file.type||"application/octet-stream",active:true,removedAt:null}});await deleteObject(current.objectKey).catch(()=>undefined);await audit({actorId:admin.id,action:"ARTIFACT_REPLACED",targetType:"ProductArtifact",targetId:id,metadata:{sha256,sizeBytes:file.size,oldObjectKey:"[REDACTED]"}});return NextResponse.json({id:artifact.id,sha256})}catch(e){return apiError(e)}}
export async function DELETE(request:Request,{params}:{params:Promise<{id:string}>}){try{assertSameOrigin(request);const admin=await requireAdmin();const{id}=await params;const artifact=await db.productArtifact.update({where:{id},data:{active:false,removedAt:new Date()}});await audit({actorId:admin.id,action:"ARTIFACT_REMOVED",targetType:"ProductArtifact",targetId:id,metadata:{productId:artifact.productId}});return NextResponse.json({ok:true})}catch(e){return apiError(e)}}

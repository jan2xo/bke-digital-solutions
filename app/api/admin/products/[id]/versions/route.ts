import { NextResponse } from "next/server";import { createHash,randomUUID } from "node:crypto";import { extname } from "node:path";import { requireAdmin } from "@/lib/auth";import { db } from "@/lib/db";import { assertSameOrigin } from "@/lib/security/request";import { uploadObject } from "@/lib/storage";import { audit } from "@/lib/audit";import { apiError } from "@/lib/http";import { z } from "zod";
import { env } from "@/lib/env";const fields=z.object({version:z.string().regex(/^\d+\.\d+\.\d+(?:-[0-9A-Za-z.-]+)?$/),releaseNotes:z.string().max(10000).default(""),operatingSystem:z.enum(["Windows","macOS","Linux"]),architecture:z.enum(["x64","arm64","universal"]),publish:z.enum(["true","false"]).default("false"),latest:z.enum(["true","false"]).default("false")});const allowed=new Set([".exe",".msi",".dmg",".pkg",".zip",".deb",".appimage"]);const MAX=env.MAX_ARTIFACT_BYTES;
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){try{assertSameOrigin(request);const admin=await requireAdmin();const{id}=await params;const form=await request.formData();const file=form.get("installer");if(!(file instanceof File))throw new Error("INSTALLER_REQUIRED");if(file.size<1||file.size>MAX)throw new Error("INVALID_FILE_SIZE");const extension=extname(file.name).toLowerCase();if(!allowed.has(extension))throw new Error("INVALID_FILE_TYPE");const input=fields.parse(Object.fromEntries([...form.entries()].filter(([key])=>key!=="installer")));const product=await db.product.findUnique({where:{id}});if(!product)throw new Error("NOT_FOUND");const bytes=new Uint8Array(await file.arrayBuffer());const sha256=createHash("sha256").update(bytes).digest("hex");const objectKey=`products/${id}/${input.version}/${randomUUID()}${extension}`;await uploadObject(objectKey,bytes,file.type||"application/octet-stream");const version=await db.$transaction(async tx=>{if(input.latest==="true")await tx.productVersion.updateMany({where:{productId:id},data:{isLatest:false}});return tx.productVersion.create({data:{productId:id,version:input.version,releaseNotes:input.releaseNotes,operatingSystem:input.operatingSystem,architecture:input.architecture,active:input.publish==="true",publishedAt:input.publish==="true"?new Date():null,isLatest:input.latest==="true",artifacts:{create:{productId:id,name:file.name,objectKey,sha256,sizeBytes:file.size,contentType:file.type||"application/octet-stream",active:input.publish==="true"}}}})});await audit({actorId:admin.id,action:"PRODUCT_VERSION_UPLOADED",targetType:"ProductVersion",targetId:version.id,metadata:{productId:id,version:input.version,sha256,sizeBytes:file.size,operatingSystem:input.operatingSystem,architecture:input.architecture}});return NextResponse.json({id:version.id,version:version.version,sha256,sizeBytes:file.size},{status:201})}catch(e){return apiError(e)}}

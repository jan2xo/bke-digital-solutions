import { ResendVerificationButton } from "@/components/resend-verification-button";
export default async function VerifyEmailPage({searchParams}:{searchParams:Promise<{delivery?:string}>}) {const{delivery}=await searchParams;return <section className="mx-auto max-w-xl px-4 py-24 text-center"><h1 className="text-4xl font-black">Check your email.</h1><p className="mt-4 text-slate-600">We sent a one-time verification link. It expires in 30 minutes and can only be used once.</p><p className="mt-3 text-sm text-slate-500">Nothing arrived? Check your spam folder, then request a new link below.</p><ResendVerificationButton deliveryFailed={delivery==="failed"}/></section>; }

import { MfaChallengeForm } from "@/components/mfa-challenge-form";
export default async function MfaLoginPage({searchParams}:{searchParams:Promise<{delivery?:string;reference?:string}>}){const{delivery,reference}=await searchParams;return <section className="mx-auto max-w-md px-4 py-16"><h1 className="text-4xl font-black">Administrator verification</h1><p className="my-4 text-slate-600">Enter the six-digit code sent to your registered email address. Use only the newest email. A single-use recovery code also works.</p>{reference&&<p className="mb-4 rounded-lg border border-blue-300 bg-blue-50 p-3 text-sm">Verification email reference: <strong>{reference}</strong></p>}{delivery==="failed"&&<p role="alert" className="mb-4 rounded-lg border border-amber-400 bg-amber-50 p-3 text-sm">Email delivery is currently unavailable. Use a saved recovery code or request another email.</p>}<MfaChallengeForm initialReference={reference}/></section>}

# V2 Web Host

This is the future Digital Solutions V2 web composition surface.

It intentionally contains no business logic yet. Real presentation enters only after a production capability exposes a presentation-ready contract. The web host must not import module Prisma, repositories, private domain objects or another provider implementation.

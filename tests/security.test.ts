import { describe,expect,it } from "vitest";
Object.assign(process.env,{NODE_ENV:"test",APP_URL:"http://localhost:3000",DATABASE_URL:"postgresql://test:test@localhost/test",SESSION_SECRET:"s".repeat(32),LICENSE_PEPPER:"l".repeat(32),EMAIL_FROM:"test@example.com",S3_BUCKET:"test",CRON_SECRET:"c".repeat(32)});
describe("security primitives",()=>{it("hashes the same license deterministically without exposing it",async()=>{const{hashLicenseKey}=await import("@/lib/security/crypto");expect(hashLicenseKey("BKE-SECRET")).toBe(hashLicenseKey("BKE-SECRET"));expect(hashLicenseKey("BKE-SECRET")).not.toContain("SECRET")});it("redacts nested secrets",async()=>{const{redact}=await import("@/lib/audit");expect(redact({safe:"ok",nested:{password:"bad",licenseKey:"bad"}})).toEqual({safe:"ok",nested:{password:"[REDACTED]",licenseKey:"[REDACTED]"}})});it("rejects hostile origins",async()=>{const{assertSameOrigin}=await import("@/lib/security/request");expect(()=>assertSameOrigin(new Request("http://localhost:3000",{headers:{origin:"https://evil.example"}}))).toThrow("INVALID_ORIGIN")})});

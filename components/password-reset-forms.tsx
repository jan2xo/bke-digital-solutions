"use client";
import { FormEvent,useState } from "react";
export function ForgotPasswordForm(){const[sent,setSent]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=new FormData(e.currentTarget);await fetch("/api/auth/password-reset/request",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email:form.get("email")})});setSent(true)}if(sent)return <p className="card p-5">If that account exists, a reset link has been sent.</p>;return <form className="card grid gap-4 p-6" onSubmit={submit}><label className="label">Email<input className="input" name="email" type="email" required autoComplete="email"/></label><button className="button">Send reset link</button></form>}
export function ResetPasswordForm({token}:{token:string}){const[message,setMessage]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=new FormData(e.currentTarget);const response=await fetch("/api/auth/password-reset/consume",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({token,password:form.get("password")})});setMessage(response.ok?"Password changed. You can now sign in.":"This reset link is invalid or expired.")}return <form className="card grid gap-4 p-6" onSubmit={submit}><label className="label">New password<input className="input" name="password" type="password" minLength={12} required autoComplete="new-password"/></label><button className="button">Change password</button>{message&&<p role="status">{message}</p>}</form>}

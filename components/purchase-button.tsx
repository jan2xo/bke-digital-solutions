"use client";
import { useState } from "react";
export function PurchaseButton({accountId,priceId}:{accountId:string;priceId:string}){const[error,setError]=useState("");const[busy,setBusy]=useState(false);async function buy(){setBusy(true);setError("");const response=await fetch("/api/checkout",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({accountId,items:[{priceId,quantity:1}]})});const body=await response.json();if(!response.ok){setError(body.error??"Checkout could not be started");setBusy(false);return}window.location.assign(body.checkoutUrl)}return <><button className="button mt-7" disabled={busy} onClick={buy}>{busy?"Starting checkout…":"Purchase securely"}</button>{error&&<p className="mt-3 text-sm text-red-700" role="alert">{error}</p>}</>}

"use client";import{FormEvent,useState}from"react";import{useRouter}from"next/navigation";export function AdminArtifactActions({id,active}:{id:string;active:boolean}){const router=useRouter();const[message,setMessage]=useState("");async function replace(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;const r=await fetch(`/api/admin/artifacts/${id}`,{method:"PATCH",body:new FormData(form)});setMessage(r.ok?"Replaced":"Replace failed");if(r.ok){form.reset();router.refresh()}}async function remove(){if(!confirm("Remove this artifact from customer downloads?"))return;const r=await fetch(`/api/admin/artifacts/${id}`,{method:"DELETE"});if(r.ok)router.refresh()}return <div className="grid min-w-48 gap-2">{active&&<button className="text-left text-xs font-bold text-red-700" onClick={remove}>Remove</button>}<form onSubmit={replace} className="grid gap-1"><input type="file" name="installer" accept=".exe,.msi,.dmg,.pkg,.zip,.deb,.AppImage" required className="text-xs"/><button className="rounded bg-[#213A53] px-2 py-1 text-xs text-white">Replace</button></form>{message&&<small>{message}</small>}</div>}

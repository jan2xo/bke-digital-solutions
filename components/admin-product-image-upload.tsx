"use client";import{FormEvent,useState}from"react";import{useRouter}from"next/navigation";export function AdminProductImageUpload({productId}:{productId:string}){const router=useRouter();const[message,setMessage]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;const r=await fetch(`/api/admin/products/${productId}/image`,{method:"POST",body:new FormData(form)});setMessage(r.ok?"Image uploaded":"Image upload failed");if(r.ok){form.reset();router.refresh()}}return <form onSubmit={submit} className="mt-3 flex flex-wrap items-end gap-2"><label className="label">Product image<input className="input" type="file" name="image" accept=".png,.jpg,.jpeg,.webp" required/></label><button className="button">Upload image</button>{message&&<small>{message}</small>}</form>}

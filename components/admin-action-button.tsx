"use client";
import{useState}from"react";import{useRouter}from"next/navigation";
export function AdminActionButton({url,label,body,method="PATCH",danger=false,confirmText}:{url:string;label:string;body?:Record<string,unknown>;method?:"PATCH"|"POST"|"DELETE";danger?:boolean;confirmText?:string}){const router=useRouter();const[busy,setBusy]=useState(false);const[error,setError]=useState("");async function run(){if(confirmText&&!confirm(confirmText))return;setBusy(true);setError("");const response=await fetch(url,{method,headers:body?{"content-type":"application/json"}:undefined,body:body?JSON.stringify(body):undefined});setBusy(false);if(!response.ok){setError("Action failed");return}router.refresh()}return <span className="inline-flex flex-col"><button type="button" disabled={busy} onClick={run} className={`rounded-lg border px-3 py-1.5 text-xs font-bold ${danger?"border-red-300 text-red-700 hover:bg-red-50":"border-[#2D5579] text-[#213A53] hover:bg-[#213A53] hover:text-white"}`}>{busy?"Working…":label}</button>{error&&<small className="text-red-700">{error}</small>}</span>}

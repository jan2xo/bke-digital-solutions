"use client";
import { useState } from "react";
export function CheckoutStartButton({purchasePlanId}:{purchasePlanId:string}){const[busy,setBusy]=useState(false);const[error,setError]=useState("");async function start(){setBusy(true);setError("");const response=await fetch("/api/checkout",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({purchasePlanId})});const payload=await response.json();if(!response.ok){setError(payload.error??"Checkout could not be started");setBusy(false);return}window.location.assign(payload.checkoutUrl)}return <><button className="button mt-6" disabled={busy} onClick={start}>{busy?"Starting secure checkout…":"Continue to secure payment"}</button>{error&&<p role="alert" className="mt-3 text-sm font-bold text-red-700">{error}</p>}</>}

"use client";
import { FormEvent,useState } from "react";
export function MagicLinkForm(){const[sent,setSent]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();await fetch("/api/auth/magic/request",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});setSent(true)}return sent?<p className="rounded-lg bg-emerald-50 p-4 text-sm text-emerald-950">If an account exists, a secure login link is on its way.</p>:<form method="post" onSubmit={submit} className="mt-5 grid gap-3"><label className="label">Email<input className="input" type="email" name="email" required autoComplete="email"/></label><button className="button secondary" type="submit">Email me a secure login link</button></form>}
